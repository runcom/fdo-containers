apiVersion: v1
kind: Service
metadata:
  name: fdo-manufacturing-server 
  labels:
    app: fdo-manufacturing-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: fdo-manufacturing-server