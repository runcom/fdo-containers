apiVersion: v1
kind: Service
metadata:
  name: fdo-owner-onboarding-server 
  labels:
    app: fdo-owner-onboarding-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP
  selector:
    app: fdo-owner-onboarding-server