apiVersion: v1
kind: Service
metadata:
  name: fdo-rendezvous-server 
  labels:
    app: fdo-rendezvous-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8082
    protocol: TCP
  selector:
    app: fdo-rendezvous-server